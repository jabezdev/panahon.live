services:
  panahon-live:
    build:
      context: .
      dockerfile: Dockerfile
    image: panahon-live:latest
    container_name: panahon-live
    ports:
      - "${HOST_PORT:-8081}:80"
    restart: unless-stopped
